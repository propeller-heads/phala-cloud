repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.10
    hooks:
      - id: ruff-format
        args: [--check]
        files: ^python/
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
        files: ^python/
